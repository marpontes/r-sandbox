setwd("~/Documents/Pessoal/course_challenges/week06/lecture_netflix")
movies = read.table("i.item.txt", header=FALSE, sep="|",quote="\"")
setwd("~/Documents/Pessoal/course_challenges/week06/lecture_netflix")
movies = read.table("i.item.txt", header=FALSE, sep="|",quote="\"")
movies = read.table("u.item.txt", header=FALSE, sep="|",quote="\"")
str(movies)
colnames(movies) = c("ID", "Title", "ReleaseDate", "VideoReleaseDate", "IMDB", "Unknown", "Action", "Adventure", "Animation", "Childrens", "Comedy", "Crime", "Documentary", "Drama", "Fantasy", "FilmNoir", "Horror", "Musical", "Mystery", "Romance", "SciFi", "Thriller", "War", "Western")
str(movies)
# Remove unnecessary variables
movies$ID = NULL
movies$ReleaseDate = NULL
movies$VideoReleaseDate = NULL
movies$IMDB = NULL
# Remove duplicates
movies = unique(movies)
# Take a look at our data again:
str(movies)
table(movies$Comedy)
table(movies$Western)
table(movies$Romance,movies$Drama)
distances = dist(movies[2:20], method = "euclidean")
clusterMovies = hclust(distances, method = "ward")
# Plot the dendrogram
plot(clusterMovies)
# Assign points to clusters
clusterGroups = cutree(clusterMovies, k = 10)
#Now let's figure out what the clusters are like.
tapply(movies$Action, clusterGroups, mean)
tapply(movies$Romance, clusterGroups, mean)
subset(movies, Title=="Men in Black (1997)")
clusterGroups[257]
# Create a new data set with just the movies from cluster 2
cluster2 = subset(movies, clusterGroups==2)
# Look at the first 10 titles in this cluster:
cluster2$Title[1:10]
clusterGroups2 = cutree(clusterMovies, k = 2)
tapply(movies$Romance, clusterGroups, mean)
tapply(movies$Romance, clusterGroups2, mean)
tapply(movies[2:20], clusterGroups2, mean)
tapply(movies[2], clusterGroups2, mean)
table(clusterGroups2)
table(movies,clusterGroups2)
table(movies$Romance,clusterGroups2)
table(movies[2:20],clusterGroups2)
table(movies[2:20])
df2 = cbind(movies,clusterGroups2)
table(movies$clusterGroups2)
str(df2)
table(df2$clusterGroups2)
table(movies[2:20],df2$clusterGroups2)
df2 = subset(df2,clusterGroups2 == 2)
str(df2)
